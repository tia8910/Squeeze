<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device through backup or device-to-device transfer.

  Cloud backup would put an encrypted measurement database on Google's servers, which
  contradicts the guarantee this app makes. The user's route to a backup is the app's own
  passphrase-encrypted export, which they control and can store where they choose.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
