<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      INTERNET is required by the AdMob SDK and by nothing else in this app.

      It is worth being precise about what that does and does not change. No measurement,
      photo, or training record is ever transmitted: there is no backend to receive one,
      and no code path that opens a socket outside the ad and billing SDKs. What the
      permission does cost is verifiability — a user can no longer confirm the claim by
      reading the permission list alone, which is why the source is published under the
      GPL and builds are reproducible.

      Ads are also kept structurally away from health data: see AdGate, which refuses to
      serve on any screen showing body composition. Google Play's Health Apps policy
      prohibits using health data for advertising, and the cleanest way to comply is to
      never put the two in the same place.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Photo capture for silhouette measurement. Declared optional so the app still
         installs on devices without a camera, where tape entry remains fully functional. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Play Billing merges com.android.vending.BILLING in from the library manifest. -->

    <application
        android:name=".SqueezeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Squeeze"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Squeeze"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <!--
          Ad ID collection is disabled. The advertising identifier is a cross-app tracking
          key, and an app holding body composition data has no business contributing to
          one. Ads still serve; they serve contextually rather than behaviourally, which
          is also the only defensible position under the Health Apps policy.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.flag.OPTIMIZE_INITIALIZATION"
            android:value="true" />
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />
    </application>

</manifest>
